
# demo-bet frontend (simple demo)

This is a zero-build demo frontend using React + Tailwind via CDN.
Open the file `frontend/index.html` in your browser.

For full local testing, run the backend first:
1. cd backend
2. npm install
3. node server.js

Then open frontend/index.html in the browser (it will call http://localhost:3000).
